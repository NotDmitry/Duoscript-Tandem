# Дата: 2026-03-19

## **Что было сделано:**

- Начала работу над **MeaningMatcher**:
  - Добавила **типы** и **начальные мок-данные** для всех тем  
    (`feat: add MeaningMatcher types and initial mock data for all topics`).
  - Реализовала **базовое перетаскивание карточек**  
    (`feat: implement basic MeaningMatcher drag and drop (WIP)`) с помощью **dnd-kit**:
    - карточки можно **перетаскивать между drop-зонами**;
    - карточки можно **возвращать на исходные позиции**;
    - базовая логика сопоставления подготовлена.
    - после нажатия кнопки **Check** карточки подсвечиваются:
      - **зелёный** — если ответ правильный;
      - **красный** — если ответ неправильный.
- Согласовала использование dnd-kit с командой — команда не возражает.

## **Проблемы:**

- Полная логика **проверки всех тем и сопоставления карточек** пока не реализована, но базовый drag & drop работает корректно.

## **Решения / Планы:**

- Завтра:
  - Написать **кастомный хук** и подключить **API**.
  - Доделать **мок-данные для всех тем**.
  - Полностью протестировать функционал перетаскивания и сопоставления карточек.

## **Затраченное время:**

- ~6 часов
