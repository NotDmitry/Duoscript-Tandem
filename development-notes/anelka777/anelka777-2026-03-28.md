# Дата: 2026-03-28

## **Что было сделано:**

- После отправки PR с **MeaningMatcher (с мок-данными)** уточнила у backend-разработчика (Дмитрия) логику **сохранения результатов виджета**.
  - Обсудили, что сохранение может быть реализовано через **callback**, либо напрямую из виджета — окончательное решение пока не определено.

- Реализовала **локальное сохранение результатов** (как предполагаемый вариант интеграции), чтобы заранее подготовить логику:
  - продумала структуру данных и поведение после завершения виджета;
  - подготовила решение, которое можно адаптировать после финализации backend-части.

- Сформулировала предложения по доработке backend-моделей и API:
  - предложила добавить новый тип виджета:  
    `WidgetType = 'quiz' | 'meaning-matcher'`;
  - описала возможную структуру `MeaningMatcherConfig` (по аналогии с QuizConfig);
  - предложила вариант endpoint для сохранения прогресса:  
    `POST /progress/meaning-matcher { userId, lessonId }`.

- Передала все предложения Дмитрию для согласования.
- Все изменения пока оставлены **локально** по просьбе backend-разработчика (до уточнения архитектуры).

## **Проблемы:**

- Отсутствует финальная структура backend-логики для MeaningMatcher, из-за чего невозможно полноценно завершить интеграцию.
- Работа ведётся с **мок-данными**, что усложняет уверенность в корректности финального поведения.

## **Решения / Планы:**

- Дождаться решения по backend-архитектуре и способу сохранения (callback или прямой запрос). Если Дмитрий не реализует это сам то:
- После согласования:
  - адаптировать текущую реализацию под API;
  - протестировать сохранение результатов с реальными данными;
  - отправить обновлённый PR

## **Затраченное время:**

- ~4 часа
