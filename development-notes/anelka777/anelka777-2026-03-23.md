# Дата: 2026-03-23

## **Что было сделано:**

- Реализовала **кастомный хук** и подключила **API**:
  - данные теперь получаются через API и обрабатываются внутри хука  
    (`feat: add api, connect data via hook, align types with backend`).

- Провела **рефакторинг структуры данных и компонентов**:
  - разделила `MatchingData` и props для более чистой архитектуры;
  - добавила `TOPIC_LABELS` и состояние завершённого результата (Completed)  
    (`refactor: split MatchingData from props, add TOPIC_LABELS and Completed result`).

- Улучшила **структуру UI и навигацию**:
  - добавила заголовок виджета и улучшила иерархию header  
    (`feat: add widget title and improve header hierarchy`);
  - реализовала навигацию по уровням, прогресс и выделила логику в `LevelGame`  
    (`feat: add level nav, progress, extract hook and LevelGame`).

- Обновила **мок-данные**:
  - переработала структуру данных для всех тем;
  - привела формат к более удобному для работы с API.

## **Проблемы:**

- Потребовалось изменить структуру данных, чтобы согласовать фронтенд с API, что затронуло несколько компонентов.

## **Решения / Планы:**

- Реализовать **сохранение результатов пользователя** (прогресс, ответы).
- При необходимости доработать API-интеграцию и обработку состояний.

## **Затраченное время:**

- ~8-10 часов
